@using Msh.Admin.Models
@using Msh.Admin.Services

@inject IAdminVmService AdminVmService
@model string

@{
	// Model is the name of the menu item. Set the corresponding menu item Active property
	var list = AdminVmService.SetActiveItem(@Model, [
		new AdminMenuItem { Name = "CcIndex", Controller = "CoinCorner", Action = "Index", Text = "Coin Corner Home" },
		new AdminMenuItem { Name = "CcConfig", Controller = "CoinCorner", Action = "Config", Text = "CC Config" }
	]);
	// 
	await Html.RenderPartialAsync("Admin/Menus/_AdminMenus", list);
}
